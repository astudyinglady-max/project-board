# 스프린트 1 이슈 목록

**프로젝트**: project-board — AI 네이티브 개발 워크플로우 관리 툴킷
**스프린트**: Sprint 1
**작성일**: 2026-03-13
**총 예상 소요**: 약 40h

> ⚠️ `docs/요구사항.md`가 아직 작성되지 않았습니다 (Phase 1 대기 중).
> 이 목록은 README.md, config.json, 현재 코드베이스를 기반으로 작성된 초안입니다.
> Phase 1 완료 후 요구사항.md 기반으로 재검토 및 보완이 필요합니다.

---

## 요약

| 라벨 | 이슈 수 | 예상 소요 |
|------|---------|-----------|
| `기획` | 3 | 4h |
| `디자인` | 3 | 5.5h |
| `프론트` | 7 | 19h |
| `백엔드` | 5 | 11.5h |
| **합계** | **18** | **40h** |

---

## 기획 이슈

### [기획-01] 프로젝트 초기 설정 문서 확정
- **라벨**: `기획`, `docs`
- **예상 소요**: 1h
- **설명**: CLAUDE.md 검토 및 코딩 컨벤션 확정. 프로젝트 목적, 금지 사항, 커밋/PR 규칙이 명확히 정의되어 있는지 확인한다.
- **수용 기준**:
  - [ ] CLAUDE.md에 코딩 컨벤션이 명확히 정의되어 있음
  - [ ] README.md에 주요 기능 및 로컬 실행 방법이 포함되어 있음
- **워크플로우 연결**: `p1s0`

---

### [기획-02] 요구사항 & 차별화 정의 ★QA
- **라벨**: `기획`
- **예상 소요**: 2h
- **설명**: Must Have / Should Have / Nice to Have 3단계로 기능을 분류하고 MVP 범위를 확정한다. 유저스토리와 차별화 포인트도 함께 작성한다.
- **산출물**: `docs/요구사항.md`, `docs/유저스토리.md`, `docs/차별화.md`
- **수용 기준**:
  - [ ] 요구사항이 3단계 우선순위로 분류되어 있음
  - [ ] 유저스토리가 `As a [사용자], I want to [행동], So that [목적]` 형식으로 작성됨
  - [ ] 차별화.md에 기존 솔루션 대비 강점이 명시되어 있음
- **워크플로우 연결**: `p1s1`

---

### [기획-03] 기술 스택 선택 근거 문서화 ★QA
- **라벨**: `기획`, `docs`
- **예상 소요**: 1h
- **설명**: 현재 사용 중인 기술(Pure HTML/CSS/JS, localStorage, GitHub Pages)의 선택 이유를 문서화하고, 프론트/백엔드/인프라 카테고리로 폴더 구조도 정리한다.
- **산출물**: `docs/기술스택.md`, `docs/폴더구조.md`
- **수용 기준**:
  - [ ] 각 기술마다 선택 이유와 대안 비교가 포함되어 있음
  - [ ] 폴더 구조가 실제 디렉토리 트리 형태로 문서화되어 있음
- **워크플로우 연결**: `p1s2`

---

## 디자인 이슈

### [디자인-01] 디자인 시스템 정의
- **라벨**: `디자인`
- **예상 소요**: 2h
- **설명**: 3개 HTML 파일(index.html, workflow.html, project-board.html)에 적용할 통일된 디자인 시스템을 정의한다. CSS Custom Properties 기반으로 컬러, 타이포그래피, 간격 시스템을 확정한다.
- **산출물**: `docs/디자인.md`
- **수용 기준**:
  - [ ] 컬러 시스템(Primary, Secondary, Neutral, Status 색상) 정의
  - [ ] 타이포그래피 스케일(h1~h6, body, caption) 정의
  - [ ] 4px 기반 간격 시스템 정의
  - [ ] 컴포넌트 규칙(버튼, 인풋, 카드, 모달) 정의
- **워크플로우 연결**: `p2s0`

---

### [디자인-02] 컴포넌트 목록 설계
- **라벨**: `디자인`
- **예상 소요**: 1.5h
- **설명**: workflow.html과 project-board.html에서 사용되는 UI 컴포넌트를 목록화하고, 각 컴포넌트의 상태(default/hover/focus/disabled)와 사용 예시를 정의한다.
- **산출물**: `docs/컴포넌트목록.md`
- **수용 기준**:
  - [ ] 모든 공통 컴포넌트가 열거되어 있음
  - [ ] 각 컴포넌트의 상태 변화가 정의되어 있음
- **워크플로우 연결**: `p2s1`

---

### [디자인-03] 페이지 & 사용자 흐름 설계 ★QA
- **라벨**: `디자인`
- **예상 소요**: 2h
- **설명**: index.html, workflow.html, project-board.html 3개 페이지의 레이아웃 구성과 핵심 사용자 시나리오 흐름을 문서화한다.
- **산출물**: `docs/페이지목록.md`, `docs/사용자흐름.md`
- **수용 기준**:
  - [ ] 각 페이지의 URL(GitHub Pages), 레이아웃, 필요 컴포넌트가 정의되어 있음
  - [ ] 핵심 시나리오 3가지(신규 사용자 온보딩, 워크플로우 진행, 프로젝트 보드 관리)가 단계별로 서술됨
- **워크플로우 연결**: `p2s2`

---

## 프론트 이슈

### [프론트-01] index.html — 랜딩 페이지 리뉴얼
- **라벨**: `프론트`, `enhancement`
- **예상 소요**: 3h
- **설명**: 현재 랜딩 페이지를 브랜드 아이덴티티에 맞게 개선한다. 히어로 섹션, 주요 기능 소개(workflow.html, project-board.html 진입점), 사용 방법 섹션을 포함한다.
- **수용 기준**:
  - [ ] 히어로 섹션에 명확한 가치 제안(Value Proposition)이 있음
  - [ ] workflow.html, project-board.html 진입 CTA가 포함되어 있음
  - [ ] 모바일 반응형(360px) 정상 동작
  - [ ] 다크 모드 지원 (CSS Custom Properties 활용)

---

### [프론트-02] workflow.html — 단계별 프롬프트 UI 개선
- **라벨**: `프론트`, `enhancement`
- **예상 소요**: 3h
- **설명**: 워크플로우 가이드 6단계 표시, 단계별 Claude Code 프롬프트 복사 기능, QC 체크리스트, 진행 상태 localStorage 저장 기능의 UI/UX를 개선한다.
- **수용 기준**:
  - [ ] 6개 Phase 진행 상태가 시각적으로 구분됨 (완료/진행중/대기)
  - [ ] 프롬프트 복사 버튼이 클릭 후 피드백(토스트 또는 버튼 변화)을 제공함
  - [ ] QC 체크리스트 체크 상태가 localStorage에 저장됨
  - [ ] 페이지 새로고침 후 진행 상태가 유지됨

---

### [프론트-03] project-board.html — 칸반 보드 핵심 기능 구현
- **라벨**: `프론트`, `enhancement`
- **예상 소요**: 4h
- **설명**: Backlog / In Progress / In Review / QC / Done 5개 컬럼 칸반 보드를 구현한다. 드래그 앤 드롭으로 카드 이동, 이슈 생성/수정/삭제, 라벨 필터링 기능을 포함한다.
- **수용 기준**:
  - [ ] 5개 컬럼이 올바르게 표시됨
  - [ ] 카드를 드래그하여 컬럼 간 이동 가능
  - [ ] 이슈 생성 시 제목, 라벨(기획/디자인/프론트/백엔드), 예상 소요 시간 입력 가능
  - [ ] 라벨별 필터링 기능 작동
  - [ ] 전체 상태가 localStorage에 저장/복원됨

---

### [프론트-04] JSON 백업 & 복원 기능
- **라벨**: `프론트`, `enhancement`
- **예상 소요**: 1.5h
- **설명**: project-board.html에서 전체 보드 상태를 JSON으로 내보내기(Export)하고, JSON 파일을 불러와 상태를 복원(Import)하는 기능을 구현한다.
- **수용 기준**:
  - [ ] Export 버튼 클릭 시 `pb_state_YYYY-MM-DD.json` 파일 다운로드
  - [ ] Import 시 파일 유효성 검사 후 상태 복원
  - [ ] Import 전 현재 데이터 덮어쓰기 경고 다이얼로그 표시

---

### [프론트-05] 공통 컴포넌트 — 토스트 알림 & 모달
- **라벨**: `프론트`
- **예상 소요**: 2h
- **설명**: 3개 HTML 파일에서 공통으로 사용할 토스트 알림 컴포넌트와 확인/취소 모달 컴포넌트를 Pure JS로 구현한다.
- **수용 기준**:
  - [ ] 토스트가 success / error / info 3가지 타입을 지원
  - [ ] 토스트가 3초 후 자동 사라짐
  - [ ] 모달이 키보드(ESC 키) 및 배경 클릭으로 닫힘
  - [ ] 모달이 포커스 트랩(focus trap)을 지원 (접근성)

---

### [프론트-06] GitHub Pages 배포 환경 검증
- **라벨**: `프론트`, `docs`
- **예상 소요**: 1h
- **설명**: GitHub Pages 배포 설정을 검증하고, 모든 HTML 파일이 올바른 상대 경로로 참조되는지 확인한다. 404 페이지 처리도 포함한다.
- **수용 기준**:
  - [ ] `https://astudyinglady-max.github.io/project-board`에서 index.html 정상 접근
  - [ ] workflow.html, project-board.html 링크가 모두 작동
  - [ ] 브라우저 콘솔에 404/CORS 에러 없음

---

### [프론트-07] 반응형 & 접근성 검증 ★QA
- **라벨**: `프론트`
- **예상 소요**: 1.5h
- **설명**: 3개 HTML 파일 전체에 대해 모바일(360px), 태블릿(768px), 데스크탑(1280px) 반응형과 키보드 내비게이션, 스크린 리더 호환성을 검증한다.
- **수용 기준**:
  - [ ] 3개 뷰포트에서 레이아웃 깨짐 없음
  - [ ] Tab 키로 모든 인터랙티브 요소에 접근 가능
  - [ ] 컬러 대비가 WCAG AA (4.5:1) 기준 충족

---

## 백엔드 이슈

> 이 프로젝트는 서버리스(Pure HTML + localStorage) 구조이므로, '백엔드' 범주는 **kit/CLI 툴링**과 **GitHub Actions CI/CD**를 담당합니다.

### [백엔드-01] kit/cli.mjs — 워크플로우 CLI 안정성 검증 ★QA
- **라벨**: `백엔드`
- **예상 소요**: 2h
- **설명**: `kit/cli.mjs`의 주요 명령어(status, next, complete, uncomplete, template, sync, qc, git-start, git-finish)가 모두 정상 동작하는지 검증하고, 엣지 케이스(잘못된 step ID, state.json 손상 등) 에러 처리를 보완한다.
- **수용 기준**:
  - [ ] 모든 CLI 명령어가 에러 없이 실행됨
  - [ ] 잘못된 step ID 입력 시 명확한 에러 메시지 출력
  - [ ] `sync` 명령어 실행 후 status.md, state.js가 올바르게 재생성됨

---

### [백엔드-02] state.js 브릿지 — workflow.html 상태 연동
- **라벨**: `백엔드`, `enhancement`
- **예상 소요**: 2h
- **설명**: `kit/cli.mjs sync`로 생성되는 `.workflow/state.js`가 `docs/workflow.html`에서 올바르게 로드되어 체크박스 상태를 자동 반영하는지 검증하고, 연동 오류를 수정한다.
- **수용 기준**:
  - [ ] `complete` 명령어 실행 후 workflow.html 새로고침 시 체크 상태 자동 반영
  - [ ] state.js 로드 실패 시 workflow.html이 graceful fallback으로 동작

---

### [백엔드-03] GitHub Actions CI 설정
- **라벨**: `백엔드`, `enhancement`
- **예상 소요**: 2h
- **설명**: PR 생성 시 HTML 유효성 검사(W3C), CSS Lint, JS Lint(ESLint)가 자동 실행되는 GitHub Actions 워크플로우를 설정한다.
- **산출물**: `.github/workflows/ci.yml`
- **수용 기준**:
  - [ ] PR 생성 시 CI가 자동 트리거됨
  - [ ] HTML 유효성 검사 실패 시 PR 차단
  - [ ] ESLint 오류 시 PR 차단

---

### [백엔드-04] GitHub Actions 자동 배포 설정
- **라벨**: `백엔드`, `enhancement`
- **예상 소요**: 1.5h
- **설명**: `main` 브랜치에 머지되면 GitHub Pages에 자동 배포되는 워크플로우를 설정한다.
- **산출물**: `.github/workflows/deploy.yml`
- **수용 기준**:
  - [ ] main 브랜치 머지 시 GitHub Pages 자동 배포
  - [ ] 배포 성공/실패 알림이 워크플로우 Summary에 표시됨

---

### [백엔드-05] kit/cli.mjs template 명령어 — 템플릿 파일 완성
- **라벨**: `백엔드`, `enhancement`
- **예상 소요**: 4h
- **설명**: config.json에 정의된 각 step의 템플릿 파일(`요구사항.md.tmpl`, `유저스토리.md.tmpl`, `디자인.md.tmpl` 등)을 `kit/templates/` 폴더에 작성하여 `template` 명령어가 실제로 동작하게 한다.
- **수용 기준**:
  - [ ] `node kit/cli.mjs template p1s1` 실행 시 `docs/요구사항.md` 템플릿 생성
  - [ ] 모든 ★QA 단계의 템플릿 파일이 존재함
  - [ ] 템플릿 파일에 작성 가이드 주석이 포함되어 있음

---

## 보드 운영 가이드

### 스프린트 1 WIP 제한

| 컬럼 | 최대 카드 수 |
|------|------------|
| In Progress | 3 |
| In Review | 5 |
| QC | 3 |

### 이슈 우선순위 권장 순서

1. **[기획-02]** 요구사항.md 작성 — 이후 이슈들의 기반이 됨
2. **[기획-03]** 기술스택.md 작성
3. **[디자인-01]** 디자인 시스템 정의 — 프론트 작업의 기준
4. **[프론트-03]** 칸반 보드 핵심 기능 — 스프린트 핵심 딜리버블
5. **[백엔드-05]** 템플릿 파일 완성 — CLI 완성도 향상
6. **[백엔드-03/04]** CI/CD 설정

### GitHub Projects 라벨 색상

```
기획   → #8b5cf6 (보라)
디자인 → #ec4899 (핑크)
프론트 → #0ea5e9 (하늘)
백엔드 → #10b981 (초록)
bug    → #ef4444 (빨강)
enhancement → #f59e0b (주황)
docs   → #6b7280 (회색)
```
