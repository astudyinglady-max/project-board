# 페이지 목록

**프로젝트**: project-board
**작성일**: 2026-03-13
**기반 문서**: 유저스토리.md

> 이 프로젝트는 서버 없는 정적 파일 기반입니다. 인증(로그인)이 없으며 모든 상태는 `localStorage`에 저장됩니다.

---

## 페이지 요약

| # | 파일 | 역할 | 관련 US | Sprint |
|---|------|------|---------|--------|
| 1 | `project-board.html` | 브랜딩 랜딩 페이지 | US-09 | Sprint 1 |
| 2 | `docs/index.html` | 프로젝트 보드 (칸반) | US-06, 07, 08, 14, 15 | Sprint 1~2 |
| 3 | `docs/workflow.html` | 워크플로우 가이드 | US-01, 02, 03, 04, 05, 16 | Sprint 1~2 |

---

## Page 1. 랜딩 페이지

### 기본 정보

| 항목 | 내용 |
|------|------|
| **파일** | `project-board.html` |
| **GitHub Pages URL** | `https://astudyinglady-max.github.io/project-board/` |
| **로컬 URL** | `http://localhost:8080/project-board.html` |
| **목적** | 툴킷을 처음 접한 사용자에게 핵심 가치를 전달하고 각 도구로 안내 |
| **인증 필요** | ❌ 없음 (공개 정적 페이지) |
| **관련 US** | US-09 |
| **Sprint** | Sprint 1 |

### 레이아웃 구성

```
┌─────────────────────────────────────────────┐
│  NAV                                        │ ← sticky, 높이 60px
│  [⚡ Project Board]  [링크들]  [GitHub →]  │
├─────────────────────────────────────────────┤
│                   HERO                      │ ← gradient 배경
│          뱃지 · 제목 · 부제목              │
│          [다운로드] [체험하기] [GitHub]     │
│      [stat] [stat] [stat] [stat]            │
├─────────────────────────────────────────────┤
│            TOOLS (What's included)          │ ← 흰 배경
│  ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ workflow │ │  board   │ │ landing  │   │
│  └──────────┘ └──────────┘ └──────────┘   │
├─────────────────────────────────────────────┤
│              PREVIEW MOCKUP                 │ ← 다크 배경
│  ┌─────────────────────────────────────┐   │
│  │  mockup: 3개 프로젝트 카드 미리보기  │   │
│  └─────────────────────────────────────┘   │
├─────────────────────────────────────────────┤
│            FEATURES (특징 소개)             │ ← 회색 배경
│  ┌──────┐ ┌──────┐ ┌──────┐             │
│  │feat  │ │feat  │ │feat  │  (6개)      │
│  └──────┘ └──────┘ └──────┘             │
├─────────────────────────────────────────────┤
│           HOW IT WORKS (사용 방법)          │
│   ①  ──  ②  ──  ③                       │ ← 3단계 플로우
├─────────────────────────────────────────────┤
│             INSTALL (배포 방법)             │ ← 다크 배경
│  ┌────────────────┐ ┌────────────────┐    │
│  │  코드 블록 ①   │ │  코드 블록 ②   │    │
│  └────────────────┘ └────────────────┘    │
├─────────────────────────────────────────────┤
│                  FOOTER                     │
└─────────────────────────────────────────────┘
                                   [🎮 체험하기] ← 우하단 플로팅
```

### 필요한 컴포넌트

| 컴포넌트 | 클래스 | 설명 |
|----------|--------|------|
| 네비게이션 바 | `nav`, `.nav-logo`, `.nav-links` | Sticky, 블러 배경 |
| 히어로 섹션 | `.hero`, `.hero-badge`, `.hero-btns` | 그라디언트 배경, CTA 버튼 3개 |
| 통계 뱃지 | `.hero-stat` | 숫자 4개 (HTML Files / Phases / Steps / Dependencies) |
| 도구 카드 | `.tool-card`, `.tool-btns` | 3개 카드 (workflow / board / landing) |
| 프리뷰 목업 | `.preview-wrap`, `.mock-card` | 다크 테마 UI 미리보기 |
| 피쳐 카드 | `.feat-card`, `.feat-icon` | 6개 기능 소개 카드 |
| 플로우 스텝 | `.flow-step`, `.flow-num` | 번호형 3단계 안내 |
| 코드 블록 | `.install-card`, `.copy-btn` | 복사 버튼 포함 설치 코드 |
| 버튼 | `.btn-primary`, `.btn-outline`, `.btn-green` | CTA 액션용 |
| 플로팅 버튼 | `.dl-float` | 우하단 고정 체험 링크 |
| 푸터 | `footer` | 저작권 및 링크 |

### 인증

```
인증 없음 — 완전 공개 페이지
서버 연동 없음 — 모든 링크는 상대 경로 또는 GitHub Pages URL
```

---

## Page 2. 프로젝트 보드

### 기본 정보

| 항목 | 내용 |
|------|------|
| **파일** | `docs/index.html` |
| **GitHub Pages URL** | `https://astudyinglady-max.github.io/project-board/docs/` |
| **로컬 URL** | `http://localhost:8080/docs/index.html` |
| **목적** | 멀티 프로젝트 관리 — Phase별 작업 카드 CRUD, 진행률 추적, 데이터 백업/복원 |
| **인증 필요** | ❌ 없음 (localStorage 기반 로컬 상태) |
| **관련 US** | US-06, US-07, US-08 *(Sprint 1)* · US-14, US-15 *(Sprint 2)* |
| **localStorage 키** | `pb_state` |
| **Sprint** | Sprint 1 (핵심) + Sprint 2 (드래그·필터) |

### 레이아웃 구성

```
┌─────────────────────────────────────────────────┐
│  HEADER                                         │ ← 높이 60px, 흰 배경
│  [📊 Project Board]  [내보내기] [불러오기] [+새 프로젝트]  │
├─────────────────────────────────────────────────┤
│  TABS                                           │
│  [📋 보드뷰]  [📊 통계]                         │
├────────────────┬────────────────────────────────┤
│  PROJECT LIST  │  BOARD AREA                    │
│                │                                │
│  ┌──────────┐  │  ┌──────┬──────┬──────┬──────┐│
│  │ 프로젝트A│  │  │Phase │Phase │Phase │Phase ││
│  │ 72%  ●  │  │  │  0   │  1   │  2   │  3   ││
│  └──────────┘  │  │      │      │      │      ││
│  ┌──────────┐  │  │[카드]│[카드]│[카드]│[카드]││
│  │ 프로젝트B│  │  │      │      │      │      ││
│  │ 40%  ●  │  │  │[+추가]│[+추가]│     │      ││
│  └──────────┘  │  └──────┴──────┴──────┴──────┘│
│  [+ 새 프로젝트]│                                │
│                │  ┌──────┬──────┐               │
│                │  │Phase │Phase │               │
│                │  │  4   │  5   │               │
│                │  └──────┴──────┘               │
└────────────────┴────────────────────────────────┘

※ 모바일: Project list → Board area 수직 스택 (Sprint 2, US-13)
※ Phase 컬럼: 드래그 앤 드롭으로 카드 이동 (Sprint 2, US-14)
```

#### 탭 1: 보드뷰 (기본)

```
헤더
  └── [보드뷰 탭] [통계 탭]

보드뷰
  ├── 프로젝트 목록 (좌측 사이드바)
  │     ├── 프로젝트 카드 (이름, 진행률, 역할 뱃지)
  │     └── [+ 새 프로젝트] 버튼
  └── Phase 칸반 (우측 메인)
        ├── Phase 0~5 컬럼 (각 Phase는 접기/펼치기 가능)
        │     ├── Phase 헤더 (이름, 완료율, 색상)
        │     └── Step 카드 목록
        │           ├── 완료 체크박스
        │           ├── Step 이름
        │           ├── 역할 뱃지 (기획/디자인/프론트/백엔드)
        │           ├── 메모 영역
        │           ├── 댓글 목록 + 입력
        │           └── [수정] [삭제]
        └── [+ 카드 추가] 버튼 (각 Phase 하단)
```

#### 탭 2: 통계

```
통계뷰
  ├── 전체 진행률 (퍼센트 + 프로그레스 바)
  ├── Phase별 완료율 (미니 바 차트)
  └── 역할별 작업 분포 (뱃지 + 카운트)
```

### 필요한 컴포넌트

| 컴포넌트 | 클래스 | 설명 | Sprint |
|----------|--------|------|--------|
| 헤더 | `.header` | 로고, 내보내기/불러오기/추가 버튼 | 1 |
| 탭 네비게이션 | `.tab`, `.tab.active` | 보드뷰 / 통계 전환 | 1 |
| 프로젝트 카드 | `.project-card`, `.pc-badge` | 이름, 진행률, 역할 뱃지 | 1 |
| Phase 블록 | `.phase-block`, `.phase-head` | 좌측 컬러 바, 접기/펼치기 | 1 |
| Step 카드 | `.step-item`, `.step-check` | 완료 체크, 역할, 메모, 댓글 | 1 |
| 역할 뱃지 | `.role-기획`, `.role-디자인` 등 | 담당 포지션 표시 | 1 |
| 상태 뱃지 | `.badge-active`, `.badge-done`, `.badge-hold` | 프로젝트 상태 | 1 |
| 진행률 바 | `.mini-bar` | Phase/프로젝트 완료율 | 1 |
| 폼 카드 | `.form-card` | 카드 추가/수정 폼 | 1 |
| 버튼 | `.btn-primary`, `.btn-outline`, `.btn-danger` | CRUD 액션 | 1 |
| 확인 모달 | (inline modal) | 삭제 확인 다이얼로그 | 1 |
| JSON 내보내기 | (anchor download) | `.json` 파일 다운로드 | 1 |
| JSON 불러오기 | `<input type="file">` | 파일 선택 → 상태 복원 | 1 |
| 역할 필터 | `.filter-bar` | 역할/상태 기준 카드 필터 | 2 |
| 드래그 핸들 | `.drag-handle` | HTML5 Drag & Drop API | 2 |
| 토스트 알림 | `.toast` | 저장/내보내기 완료 알림 | 2 |
| 다크 모드 토글 | `.theme-toggle` | CSS Custom Properties 교체 | 2 |

### 인증

```
인증 없음 — 로컬 브라우저 전용
상태 저장: localStorage['pb_state'] (JSON.stringify)
접근 제한: 없음 (누구나 URL로 접속 가능)
개인정보: 없음 (모든 데이터는 사용자 브라우저에만 존재)
```

---

## Page 3. 워크플로우 가이드

### 기본 정보

| 항목 | 내용 |
|------|------|
| **파일** | `docs/workflow.html` |
| **GitHub Pages URL** | `https://astudyinglady-max.github.io/project-board/docs/workflow.html` |
| **로컬 URL** | `http://localhost:8080/docs/workflow.html` |
| **목적** | Phase 0~5 단계별 작업 안내 — 프롬프트 복사, QC 체크, Git 명령어, 스프린트 로그 |
| **인증 필요** | ❌ 없음 (localStorage 기반 로컬 상태) |
| **관련 US** | US-01, US-02, US-03, US-04, US-05 *(Sprint 1)* · US-16 *(Sprint 2)* |
| **localStorage 키** | `ccwf_v5` |
| **외부 의존** | `.workflow/state.js` (CLI sync 연동, 선택적) |
| **Sprint** | Sprint 1 (핵심) + Sprint 2 (토스트) |

### 레이아웃 구성

```
┌───────────────────────────────────────────┐
│  HEADER                                   │
│  제목 · 설명 · 범례(QA/프롬프트/팁)       │
│  진행률 바 (완료 X / 전체 N)              │
│  [Phase 0] → [Phase 1] → ... → [Phase 5] │ ← 플로우 표시
├───────────────────────────────────────────┤
│  PHASE ACCORDION (Phase 0~5 반복)         │
│                                           │
│  ┌─────────────────────────────────────┐  │
│  │ Phase 헤더 (클릭 → 펼치기/접기)     │  │
│  │ [Phase 이름] [완료 N/M] [진행률 바] │  │
│  ├─────────────────────────────────────┤  │
│  │ STEP 목록                           │  │
│  │                                     │  │
│  │  ┌──────────────────────────────┐   │  │
│  │  │ STEP 헤더                    │   │  │
│  │  │ [☐] Step 이름  [산출물 태그] │   │  │
│  │  ├──────────────────────────────┤   │  │
│  │  │ STEP 본문 (펼침 시)          │   │  │
│  │  │ [산출물 파일 태그들]          │   │  │
│  │  │                              │   │  │
│  │  │ 📋 프롬프트 블록             │   │  │
│  │  │ ┌────────────────────────┐   │   │  │
│  │  │ │ 프롬프트 텍스트         │   │   │  │
│  │  │ │               [복사]   │   │   │  │
│  │  │ └────────────────────────┘   │   │  │
│  │  │                              │   │  │
│  │  │ ★QA Step인 경우:             │   │  │
│  │  │ ┌─ QC 체크리스트 ──────────┐ │   │  │
│  │  │ │ [☐] 항목 1               │ │   │  │
│  │  │ │ [☐] 항목 2               │ │   │  │
│  │  │ └──────────────────────────┘ │   │  │
│  │  └──────────────────────────────┘   │  │
│  │                                     │  │
│  │  Phase 하단 섹션:                   │  │
│  │  [📝 스프린트 로그]  [🔀 Git 명령어]│  │
│  └─────────────────────────────────────┘  │
│                                           │
│  (Phase 1~5 동일 구조 반복)               │
├───────────────────────────────────────────┤
│  [🔄 전체 초기화]                         │ ← 하단 우측
└───────────────────────────────────────────┘
```

### 필요한 컴포넌트

| 컴포넌트 | 클래스 | 설명 | Sprint |
|----------|--------|------|--------|
| 헤더 | `.header` | 제목, 전체 진행률 바, 범례 | 1 |
| Phase 플로우 표시기 | `.flow`, `.flow-item` | Phase 0~5 순서 표시 | 1 |
| Phase 아코디언 | `.phase`, `.phase-head`, `.phase-body` | 클릭으로 펼치기/접기 | 1 |
| Phase 진행률 바 | `.mini-bar` | 완료율 표시 | 1 |
| Step 항목 | `.step-head`, `.step-body` | 체크박스 + 이름 + 산출물 태그 | 1 |
| 체크박스 | `.checkbox`, `.checkbox.checked` | Step/QC 항목 완료 처리 | 1 |
| 산출물 파일 태그 | `.file-tag` | 생성해야 할 파일명 표시 | 1 |
| 프롬프트 블록 | `.prompt-block`, `.prompt-text` | 다크 배경, 복사 버튼 포함 | 1 |
| 복사 버튼 | `.copy-btn`, `.copy-btn.done` | 클릭 → "복사됨" 피드백 | 1 |
| QC 체크리스트 | `.qc-section`, `.checklist-item` | ★QA Step 전용, 색상 강조 | 1 |
| 스프린트 로그 | `.log-section`, `.log-items` | 로그 기록 항목 + 프롬프트 | 1 |
| Git 명령어 블록 | `.git-section`, `.git-code` | 다크 코드 블록, 명령어 색상 | 1 |
| 팁 박스 | `.tip` | 노란 배경 안내 박스 | 1 |
| 초기화 버튼 | `.reset-btn` | 전체 상태 초기화 (확인 필요) | 1 |
| CLI 상태 브릿지 | `state.js` script 로드 | `.workflow/state.js` 연동 | 1 |
| 토스트 알림 | `.toast` | 복사 완료 등 액션 피드백 | 2 |
| 다크 모드 토글 | `.theme-toggle` | CSS Custom Properties 교체 | 2 |

### 인증

```
인증 없음 — 로컬 브라우저 전용
상태 저장: localStorage['ccwf_v5'] (JSON.stringify)
CLI 연동: .workflow/state.js 파일 로드 (없으면 기본 상태)
접근 제한: 없음 (누구나 URL로 접속 가능)
```

---

## 공통 정책

### 인증 정책

이 프로젝트는 서버 없는 정적 파일 기반으로 **인증이 존재하지 않습니다.**

| 항목 | 내용 |
|------|------|
| 로그인 | 없음 |
| 세션 | 없음 |
| 사용자 구분 | 없음 (기기별 독립 localStorage) |
| 데이터 동기화 | JSON 내보내기/불러오기로 수동 동기화 |
| 접근 제한 | 없음 (GitHub Pages URL이면 누구나 접근 가능) |

### localStorage 구조

| 키 | 사용 페이지 | 형식 | 초기화 방법 |
|----|------------|------|------------|
| `pb_state` | `docs/index.html` | `JSON` | [🔄 초기화] 버튼 또는 DevTools |
| `ccwf_v5` | `docs/workflow.html` | `JSON` | [🔄 초기화] 버튼 또는 DevTools |

### 페이지 간 이동

```
project-board.html (랜딩)
    ├── → docs/index.html   (프로젝트 보드 CTA)
    └── → docs/workflow.html (워크플로우 CTA)

docs/index.html ↔ docs/workflow.html (내비게이션 링크)
```

---

## Sprint별 구현 범위

### Sprint 1 (Must Have)

| 페이지 | 구현 범위 |
|--------|----------|
| `project-board.html` | 랜딩 페이지 전체 (US-09) |
| `docs/index.html` | 프로젝트 CRUD, Phase 칸반, Step 체크, 메모/댓글, JSON 백업 (US-06~08) |
| `docs/workflow.html` | Phase 아코디언, 프롬프트 복사, QC 체크, localStorage 저장, CLI 연동 (US-01~05) |

### Sprint 2 (Should Have)

| 페이지 | 구현 범위 |
|--------|----------|
| `docs/index.html` | 역할/상태 필터 (US-15), 드래그 앤 드롭 (US-14) |
| `docs/workflow.html` | 토스트 알림 (US-16) |
| 전체 | 다크 모드 토글 (US-12), 모바일 반응형 (US-13) |

---

## 변경 이력

| 날짜 | 버전 | 내용 |
|------|------|------|
| 2026-03-13 | v1.0 | 초안 작성 — 유저스토리.md 기반 3개 페이지 정의 |
